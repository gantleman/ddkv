1，节点内存储的路由距离是否为正态分布，分布的高点是以自己为中心尽量向全部网络散播；
2，路由同步数据的触发节点和触发的时机；
3，如何保证数据在某些节点范围内是几乎必然会存在；
4，如何控制一个文件保存的范围；
5，如何获取一个文件的最新值；
6，添加到K桶的规则；
7，种子传播的规则；
8，反复发布和搜索会卡死没有返回结果？从现象看搜索结果没有被删除。

http://www.bittorrent.org/beps/bep_0005.html
9, token的控制功能和资源回收
10，资源没有散播到其他的节点上。
11，多线程多开DHT；
12，peer的环状计数广播；
13, 实现基本的永久存储功能；
14，写一个简单的demo使用单点发布，环形广播的机制
15, 对于新出现的节点或者消失的节点如何保证事务。
16，如果A同时发起两笔交易，并获得了两个节点作为验证节点，会不会产生双花交易。
很可能在第一节点确认时产生了一个节点里key的位子更近，并且这个两个节点都是第一次记录
的第一笔交易。如果不是第一笔交易那么需要先同步A的数据。A虽然检测到两笔几乎同时的交易但
没有提出异议，故意产生双花。如果单纯的考虑存储的问题，当A几乎同时发起两笔存储数据。
这时出现一个新的服务器，如何保证两笔数据的存储是连续并正确的。
可以在检测到双花交易时封掉帐户，帐户提供的交易号必须是连续的。
如果提供一个非连续并且重复的交易记录这个账户将被封存。
考虑一个情况用户本机数据全部丢失，那么他的序列号将有p2p网络提供。
如果返回的数据不是最新数据，那么可能会触发双花。
或者触发双花的情况下并且序列号相同，其中一个校远的记录将会被丢弃。
同时发布双花的节点会发起散播竞争，直到获得足够多的确认返回。
A发起交易扣钱方-〉B接受交易收入方-〉C记录A的交易-〉D记录B的交易
数据记录来说首先数据是顺序的，不可删除的，不个更改，可以读取。
17, 同步数据的问题，同步多少帐户，如何同步。部分同步和全部同步。